# zerion-brand-tokens data and privacy

This component is a static set of CSS color and typography values. It processes no user data, makes no network request, and has no runtime behavior of its own; it is inlined into the static HTML report that `reporting_html.py` writes and is read by the browser that opens that file. There is nothing to disclose under this heading beyond that absence.
